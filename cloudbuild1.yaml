steps:
- name: gcr.io/cloud-builders/gcloud
    args:
      - '-c'
      - >-
        gcloud run deploy merlinapp --source ./UI/ --region=asia-southeast2
        --max-instances=5 --allow-unauthenticated

options:
  logging: CLOUD_LOGGING_ONLY
